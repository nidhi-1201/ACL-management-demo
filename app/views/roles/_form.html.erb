<%= form_with(model: role) do |form| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

    <div class="field">
    <%= form.label :controller %>
    <%= controller_name %><br/>
      <%= form.label :permissions %><br />
      <%= form.collection_check_boxes :permission_ids, Permission.all, :id, :name %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
